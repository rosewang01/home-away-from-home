import express from "express";

const countryRouter = express.Router();

countryRouter.get("/all", );
countryRouter.get("/job/:job", );
countryRouter.get("/employer/:employer", );
countryRouter.get("/growth", );
countryRouter.get("/debt", );

export { countryRouter };

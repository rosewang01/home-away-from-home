import express from "express";

const cityRouter = express.Router();

cityRouter.get("/all", );
cityRouter.get("/job/:job", );
cityRouter.get("/employer/:employer", );
cityRouter.get("/growth", );
cityRouter.get("/debt", );

export { cityRouter };
